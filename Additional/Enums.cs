namespace LiteCodeLibrary.Additionals
{
    public static class Enums
    {
        public enum WindowFormStyle
        {
            Normal = 0,
            Hide = 1,
            Minimized = 2,
            Maximized = 3
        }

        public enum FileType
        {
            TextFile = 0,
            BatchFile = 1,
            BinaryFile = 2,
            LogFile = 3,
            DllFile = 4,
            ExeFile = 5
        }
    }
}
